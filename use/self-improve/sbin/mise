#!/bin/sh
# Lazy shim for mise
#
# --version / -V / -v: defers to add-mise --version.
# anything else: execs real mise if on PATH; else runs add-mise.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=mise
ADD=add-mise
shim_main "$@"
