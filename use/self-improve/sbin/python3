#!/bin/sh
# Lazy shim for python3
#
# --version / -V / -v: defers to add-python --version.
# anything else: execs real python3 if on PATH; else runs add-python.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=python3
ADD=add-python
shim_main "$@"
