#!/bin/sh
# Lazy shim for gh (GitHub CLI)
#
# --version / -V / -v: defers to add-gh --version.
# anything else: execs real gh if on PATH; else runs add-gh.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=gh
ADD=add-gh
shim_main "$@"
