#!/bin/sh
# Lazy shim for go
#
# --version / -V / -v: defers to add-go --version.
# anything else: execs real go if on PATH; else runs add-go.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=go
ADD=add-go
shim_main "$@"
