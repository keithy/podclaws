#!/bin/sh
# Lazy shim for pip (bundled with python)
#
# --version / -V / -v: defers to add-python --version.
# anything else: execs real pip if on PATH; else runs add-python.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=pip
ADD=add-python
shim_main "$@"
