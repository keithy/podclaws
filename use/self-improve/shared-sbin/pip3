#!/bin/sh
# Lazy shim for pip3 (bundled with python)
#
# --version / -V / -v: defers to add-python --version.
# anything else: execs real pip3 if on PATH; else runs add-python.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=pip3
ADD=add-python
shim_main "$@"
