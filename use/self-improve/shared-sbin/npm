#!/bin/sh
# Lazy shim for npm (bundled with node)
#
# --version / -V / -v: defers to add-node --version.
# anything else: execs real npm if on PATH; else runs add-node.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=npm
ADD=add-node
shim_main "$@"
