#!/bin/sh
# Lazy shim for claude using mise (via npm backend)

echo "[Shim] claude requested. Bootstrapping globally..." >&2

# Hit our npm lazy shim to ensure node is installed globally
npm --version >/dev/null

mise use -g npm:@anthropic-ai/claude-code@latest

exec mise exec -- claude "$@"
