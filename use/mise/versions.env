# Pinned upstream mise release used by both Alpine (musl) and Debian (glibc)
# containers. Bump this and re-run `make` to update the staged binaries in
# /srv/auto_mise-{musl,glibc}/_data/RELEASES/mise/.
MISE_VERSION=2026.6.13
