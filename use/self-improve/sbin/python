#!/bin/sh
# Lazy shim for python using mise
# This intercepts the first call to `python`, installs mise and python globally,
# and generates the mise shims. Subsequent calls will hit the mise shim instead.

echo "[Shim] python requested. Bootstrapping mise and installing globally..." >&2

mise use -g python@latest

exec mise exec -- python "$@"
