#!/bin/sh
# Lazy shim for pipx
#
# --version / -V / -v: defers to add-python --version.
# anything else: execs real pipx if on PATH; else runs add-python.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=pipx
ADD=add-python
shim_main "$@"
