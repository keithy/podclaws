#!/bin/sh
# Lazy shim for python
#
# --version / -V / -v: defers to add-python --version.
# anything else: execs real python if on PATH; else runs add-python.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=python
ADD=add-python
shim_main "$@"
