# Podclaws root compose — services defined in use/ directory
# This file provides the network config and shared named volumes.

volumes:
  mise-musl:
  mise-glibc:
  mise-cache:

networks:
  default:
    driver: bridge