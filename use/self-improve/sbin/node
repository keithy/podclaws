#!/bin/sh
# Lazy shim for node
#
# --version / -V / -v: defers to add-node --version.
# anything else: execs real node if on PATH; else runs add-node.

. "$(dirname "$0")/lib/shim-common.sh"
TOOL=node
ADD=add-node
shim_main "$@"
